version: '2'

services:
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    depends_on:
      - unews-server
      - mongo
    restart: always
  unews-parser:
    image: zeusapps/unews-parser
    depends_on:
      - unews-server
      - mongo
      - nginx
    restart: always
  unews-server:
    image: zeusapps/unews
    depends_on:
      - mongo
    restart: always
  mongo:
    image: mongo
    volumes:
      - mongo:/data/db
    restart: always
volumes:
  mongo: { }